---
name: ip-monthly-statistics
description: 填写知识产权月度统计表（专利/软著/论文/论著登记）。用户提供受理通知书、软著证书、论文发布稿要求填入登记表时使用。
version: 2.0.0
---

# 知识产权月度统计表填写

> **本 skill 分两部分**：
> - **工作要求**（下方通用部分）：表格结构、填写规范、流程——各单位通用，一般不用改。
> - **个人填写信息**（文末「配置区」）：部门、保管人、技术领域、项目类型等本单位口径——**分享给他人或换单位使用时，先按对方情况修改配置区**。

## 批量模式（scripts/，>5 份证书时用）

少量文件（1-5 份）直接按流程 read_file 逐份处理；批量时三步脚本流水线（均已在真实场景验证）：

```bash
# 1) 批量提取 PDF 文本（无文本层自动标 SCAN，需 OCR）
python scripts/extract_ip_pdf.py <证书目录或PDF> --out extract.json

# 2) 解析为结构化字段（受理/授权/软著/PCT；顿号分隔、日期归一）
python scripts/parse_ip.py extract.json --out parsed.json
#    SCAN 项用 paddle-ocr 技能 OCR（E:/Anaconda/python.exe ocr.py --dpi 200），人工核对

# 3) AI 审核 parsed.json 后整理成 records.json（修正 OCR 截断/补缺字段），写入目标表：
python scripts/fill_ip.py --records records.json --xlsx 目标表.xlsx --fix fix.json --rebuild-monthly
```

脚本安全设计：fill_ip.py 自动检测已有数据行，从下一个空行写入，**绝不覆盖**；序号自动顺延；月度统计可一键重建。

## 一、工作要求（通用）

### 表格结构（6 个分类 sheet）
1. 1-国内专利-受理（50 列）：专利受理通知书 → 此 sheet
2. 2-国内专利-授权（51 列）：专利授权证书/授权公告 → 此 sheet
3. 3-海外专利PCT阶段（32 列）
4. 4-论文（28 列）：论文发布稿
5. 5-软著（31 列）：软著登记证书 → 此 sheet
6. 6-论著（27 列）

各分类字段结构不同（专利有申请号/发明人，软著有登记号/著作权人），**严禁合并成通用单列结构**。

### 两表分工
- **累计表**（知识产权累计统计_YYYY-MM.xlsx）：自留台账，6 个分类 sheet 累积全部历史记录 + 「月度统计」sheet（月份×类别数量汇总）。持续累积不清空。
- **分月统计表**（知识产权分月统计_YYYY-MM.xlsx）：报送管理部门的模板件，= 原模板 6 个分类 sheet 原样结构（字段全，供科创系统录入），只含当期记录；每月报送新起一份，文件名带当月。
- 两表前 6 个 sheet 结构一致，差别只在累计表多「月度统计」sheet 与累积范围。

### 证书/文件 → 字段映射
| 来源 | 提取字段 |
|---|---|
| 专利受理通知书 | 专利名称、类型（发明/实用新型/外观）、申请号、申请日、申请人、发明人 |
| 专利授权证书 | 上述 + 授权公告日 |
| 软著登记证书 | 软件名称（含 V1.0）、证书编号（软著登字第xx号）、登记号（20xxSRxxxxxx）、著作权人、登记日期、权利取得方式、权利范围 |
| 论文发布稿 | 论文名称、作者、刊物、刊号、刊期、出版日期 |

### 填写规范（通用）
- 发明人/作者等多人列表一律用中文顿号"、"分隔，不用英文逗号。
- 编号保留证书原样格式（如申请号 202610995253.X，校验位前有点）。
- 日期格式 yyyy-mm-dd。
- 证书上没有的信息不留空不臆造；有歧义字段（如软著"申请人"）按最接近的证书信息填并在回复中说明。
- 非必填列一律留空（必填清单见配置区）。

### 流程
1. read_file 提取 PDF/图片文本（受理通知书/证书/论文稿）；无文本层（扫描件 chars=0）用 paddle-ocr 技能 OCR（E:/Anaconda/python.exe + ocr.py --dpi 200，输出到文件后正则抓关键行）
2. 批量场景：pymupdf 批量提取→正则解析（注意 2022 版受理通知书无发明人字段、竖排文本如"专利号"需去换行匹配、长名单跨行需块提取）→ 结构化 JSON
3. 核对「配置区」固定值与必填清单是否仍适用于本次填报（换单位/换人时必须改）
4. 用 openpyxl 填对应 sheet 新行（表头逐列匹配字段名写入），**先确认已有行数，新记录从下一个空行写起，绝不从固定行号覆盖**；序号每 sheet 内顺延
5. 保存后 dump 验证：每个 sheet 行数 = 已有 + 新增，抽查首末行与关键字段
6. 汇报填写内容 + 待确认项（如有）
7. 复制为友好文件名交付（MEDIA: 发送）：记录同时进累计表 + 当期分月统计表；如用户有 WPS 云盘/网盘归档目录，同步一份到对应目录

## 二、个人填写信息（配置区——按本单位实际情况修改）

### 固定值
| 字段 | 值 |
|---|---|
| 部门/单位 | XX服务中心（示例，按本单位改） |
| 使用保管人 | 张三（示例，按本人改） |
| 技术领域(一级) | 电气技术 |
| 技术领域(二级) | 供电、配电、用电与电气化 |
| 技术领域(三级) | 用电管理 |
| 技术领域(四级) | 其他 |
| 智能电网环节 | 智能用电 |
| 知识产权增加方式 | 科技信息项目自主开发 |
| 项目类型 | 005||科技项目 |
| 上报年份/季度 | 按当前日期所在季度（date 命令确认；1-3月第一、4-6月第二、7-9月第三、10-12月第四） |

### 必填字段（本单位口径，其余列留空）
部门/单位、序号、专利名称(软著:软件著作权名称)、专利类型、申请人、申请日期(软著:登记日期)、申请号(软著:证书编号+登记号)、使用保管人、发明/设计人(软著:著作权人)、专利权人(软著:著作权人)、上报年份、上报季度、技术领域(一级~四级)、智能电网环节(软著sheet无此列)、知识产权增加方式、项目类型

软著 sheet 额外可填：权力获得方式(原始取得)、权力范围(全部权利)。证书无"申请人"栏时按著作权人填。
